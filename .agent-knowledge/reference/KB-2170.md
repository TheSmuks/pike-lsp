---
id: KB-AUTO-2170
domain: REFACTOR
date: 2026-04-19
authors: [dga-coder]
summary: Replace inline mock connection objects with centralized createMockConnection and asConnection helpers across 8 scenario test files
keywords: createMockConnection, asConnection, MockConnection, as-unknown-cast, test-helpers, mock-services, scenario-tests
---

# KB-2170: Replace createMockConnection with bridge/parser API

## Summary

8 scenario test files had inline mock connection objects with `as unknown as Connection` casts scattered throughout. These were replaced with the centralized `createMockConnection()` from `test-helpers.ts` and the `asConnection()` helper, which centralizes the structural cast in one place. The `MockConnection` interface was expanded to include all handler types (codeAction, codeLens, callHierarchy, etc.) and the `sendDiagnostics` signature was updated to include `version`.

## Key Files Changed

- `packages/pike-lsp-server/src/tests/helpers/test-helpers.ts`: Added `asConnection()`, `asServices()`, `asTextDocuments()` cast helpers; expanded `MockConnection` interface with codeAction/codeLens/callHierarchy handlers; updated `createMockConnection()` to capture all handler registrations
- `packages/pike-lsp-server/src/tests/helpers/mock-services.ts`: Re-exports `asConnection`, `asServices`, `asTextDocuments` from test-helpers
- `packages/pike-lsp-server/src/scenarios/call-hierarchy-incomplete-position.test.ts`: Replaced `createCapturingConnection()` with `createMockConnection()` + `asConnection()`
- `packages/pike-lsp-server/src/scenarios/code-actions-parse-resilience.test.ts`: Replaced inline connection mock with `createMockConnection()` + `asConnection()`
- `packages/pike-lsp-server/src/scenarios/code-lens-parse-resilience.test.ts`: Same pattern
- `packages/pike-lsp-server/src/scenarios/document-open-race.test.ts`: Same pattern, uses both `createMockConnection` from test-helpers and `createMockServices` from mock-services
- `packages/pike-lsp-server/src/scenarios/fault-bridge-crash.test.ts`: Same pattern
- `packages/pike-lsp-server/src/scenarios/fault-bridge-restart.test.ts`: Same pattern
- `packages/pike-lsp-server/src/scenarios/fault-invariant.test.ts`: Same pattern
- `packages/pike-lsp-server/src/scenarios/context-menu.test.ts`: Not changed — file does not use `createMockConnection`, only reads `package.json` with `import.meta.url`
